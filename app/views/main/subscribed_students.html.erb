<h1>Subscribed Students</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Student ID</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subsribed_students.each do |student| %>
      <tr>
        <td><%= Subscriber.find(student.subscriber_id).fname %></td>
        <td><%= Subscriber.find(student.subscriber_id).lname %></td>
        <td><%= Subscriber.find(student.subscriber_id).insorstid %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Home', home_path %>