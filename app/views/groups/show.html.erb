<p id="notice"><%= notice %></p>

<p>
  <strong>Group name:</strong>
  <%= @group.group_name %>
</p>

<p>
  <strong>Course:</strong>
  <%= @group.course.course_name %>
</p>

<p>
  <strong>Project:</strong>
  <%= @group.project %>
</p>

<%= link_to 'Members', show_members_path(@group) if policy(@group).add_member? %> |
<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', filtered_groups_path(@group.course  ) %>
